<form method="post" action="{{ path('app_brew_boiling_delete', {'id': brew_boiling.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brew_boiling.id) }}">
    <button class="btn">Delete</button>
</form>
