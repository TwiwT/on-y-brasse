<form method="post" action="{{ path('app_brew_bottling_delete', {'id': brew_bottling.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brew_bottling.id) }}">
    <button class="btn">Delete</button>
</form>
