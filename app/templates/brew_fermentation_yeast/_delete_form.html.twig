<form method="post" action="{{ path('app_brew_fermentation_yeast_delete', {'id': brew_fermentation_yeast.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brew_fermentation_yeast.id) }}">
    <button class="btn">Delete</button>
</form>
