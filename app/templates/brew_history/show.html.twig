{% extends 'base.html.twig' %}

{% block title %}BrewHistory{% endblock %}

{% block body %}
    <h1>BrewHistory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brew_history.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ brew_history.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_brew_history_index') }}">back to list</a>

    <a href="{{ path('app_brew_history_edit', {'id': brew_history.id}) }}">edit</a>

    {{ include('brew_history/_delete_form.html.twig') }}
{% endblock %}
