<form method="post" action="{{ path('app_brew_hopping_delete', {'id': brew_hopping.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brew_hopping.id) }}">
    <button class="btn">Delete</button>
</form>
