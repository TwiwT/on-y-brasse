<form method="post" action="{{ path('app_brew_mashing_grain_delete', {'id': brew_mashing_grain.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brew_mashing_grain.id) }}">
    <button class="btn">Delete</button>
</form>
