{% extends 'base.html.twig' %}

{% block title %}Flavor index{% endblock %}

{% block body %}
    <h1>Flavor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for flavor in flavors %}
            <tr>
                <td>{{ flavor.id }}</td>
                <td>{{ flavor.name }}</td>
                <td>
                    <a href="{{ path('app_flavor_show', {'id': flavor.id}) }}">show</a>
                    <a href="{{ path('app_flavor_edit', {'id': flavor.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_flavor_new') }}">Create new</a>
{% endblock %}
