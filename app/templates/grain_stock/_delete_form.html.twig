<form method="post" action="{{ path('app_grain_stock_delete', {'id': grain_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grain_stock.id) }}">
    <button class="btn">Delete</button>
</form>
