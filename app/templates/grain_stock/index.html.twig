{% extends 'base.html.twig' %}

{% block title %}GrainStock index{% endblock %}

{% block body %}
    <h1>GrainStock index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Bought_at</th>
                <th>Weight</th>
                <th>Remaining_weight</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for grain_stock in grain_stocks %}
            <tr>
                <td>{{ grain_stock.id }}</td>
                <td>{{ grain_stock.boughtAt ? grain_stock.boughtAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ grain_stock.weight }}</td>
                <td>{{ grain_stock.remainingWeight }}</td>
                <td>
                    <a href="{{ path('app_grain_stock_show', {'id': grain_stock.id}) }}">show</a>
                    <a href="{{ path('app_grain_stock_edit', {'id': grain_stock.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_grain_stock_new') }}">Create new</a>
{% endblock %}
