{% extends 'base.html.twig' %}

{% block title %}Hop{% endblock %}

{% block body %}
    <h1>Hop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hop.name }}</td>
            </tr>
            <tr>
                <th>Acid_alpha</th>
                <td>{{ hop.acidAlpha }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hop_index') }}">back to list</a>

    <a href="{{ path('app_hop_edit', {'id': hop.id}) }}">edit</a>

    {{ include('hop/_delete_form.html.twig') }}
{% endblock %}
