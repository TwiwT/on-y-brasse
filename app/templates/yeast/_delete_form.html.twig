<form method="post" action="{{ path('app_yeast_delete', {'id': yeast.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ yeast.id) }}">
    <button class="btn">Delete</button>
</form>
