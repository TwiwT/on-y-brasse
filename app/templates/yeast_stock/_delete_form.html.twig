<form method="post" action="{{ path('app_yeast_stock_delete', {'id': yeast_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ yeast_stock.id) }}">
    <button class="btn">Delete</button>
</form>
